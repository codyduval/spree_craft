## Customize the test machine
machine:

  timezone:
    America/New_York # Set the timezeone

  # Add some environmental variables
  environment:
    CIRCLE_ENV: test

database:
  override:
    - echo 'Do nothing'

test:
  override:
    - rake


    